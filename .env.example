# Application environment: development or production
APP_ENV=development

# PostgreSQL connection settings
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=vitra_db

# Full database URL for the application
# The host `db` refers to the service name in docker-compose.yml
DATABASE_URL=postgresql+psycopg2://admin:admin@db:5432/vitra_db
